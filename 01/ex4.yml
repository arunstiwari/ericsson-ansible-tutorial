---
  - name: Facts Example
    hosts: localhost
    connection: local
    vars:
      greeting: Welcome to Ansible Session
    tasks:
      - name: Print the greeting variable        
        debug: msg="{{ greeting }}"