1. Create a directory nginx at location /tmp whose owner is admin and group is admin

2. Delete the directory nginx from the /tmp location

3. Remove the user and group admin 


group and user are part of system modules
files are part of file modules